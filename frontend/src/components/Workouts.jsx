import React from 'react';

function Workouts() {
    return (
        <div className="App">
            <h1>Workouts page under construction (pending DB setup)</h1>
        </div>
    )
}

export default React.memo(Workouts);